spring:
  application:
    name: auth-service
  cloud:
    config:
      enabled: true
      uri: http://config-service:8081

  data:
    mongodb:
      host: mongodb
      port: 27017
      database: librarydb
jwt:
  secret: secret
